# PRJ-20260418-001 monopoly3d — 地图编辑器 PRD

## 项目背景
monopoly3d 是一个 3D 地产大亨 Web 游戏（Three.js + React）。核心地图为 4×4 = 16 格正方形棋盘。

---

## 功能需求

### H. 地图编辑器

#### 1. 棋盘配置
- **格子数量**：支持 3×3、4×4、5×5、6×6 可选
- **格子类型**：
  - 空地（无事件）
  - 地产地皮（可购买/升级）
  - 机遇卡格
  - 命运卡格
  - 起点
  - 监狱/路过坐牢
  - 停车场（免费停留）
  - 税务局
  - 自由出入
- **格子颜色**：预设色盘 + 自定义 HEX 颜色
- **格子名称**：可编辑文字标签

#### 2. 地产配置（针对"地产地皮"类型）
- 每格地产可配置：
  - 名称（默认"第N格"）
  - 价格（默认 $100）
  - 每次路过费用（默认 $50）
  - 颜色组（用于同色组全部拥有时的加成）
  - 等级区间（0=无建筑, 1=房屋, 2=旅馆, 3=酒店）

#### 3. 规则配置
- 初始资金（默认 $1500）
- 最大玩家数（2-6）
- 回合超时时间（0=无限制）
- 是否开启机遇/命运卡
- 是否开启房产税/过路费

#### 4. 预览
- 实时 3D 预览地图（与游戏主视角一致）
- 点击格子可编辑
- 拖拽旋转预览

#### 5. 保存/导出
- 保存到 localStorage（最多存 10 个）
- 导出 JSON 文件下载
- 导入 JSON 文件
- 设为默认地图

#### 6. 预设模板
- 经典模式（4×4，16格标准配置）
- 简化模式（3×3，9格）
- 大地图模式（5×5，25格）

---

## UI 布局

```
+--------------------------------------------------+
|  地图编辑器                          [保存] [导出]|
+--------------------------------------------------+
|  左侧面板 (280px)  |  主区域 (3D Canvas)          |
|  - 棋盘尺寸选择     |                              |
|  - 预设模板列表     |    [3D 棋盘预览/编辑]         |
|  - 规则配置         |                              |
|  - 格子属性面板     |                              |
|    (选中格时显示)   |                              |
+--------------------------------------------------+
|  底部工具栏：[选中工具] [撤销] [重做] [重置]       |
+--------------------------------------------------+
```

---

## 技术方案
- Three.js：3D 渲染（同主游戏）
- React + Zustand：编辑器 UI 状态
- localStorage：地图数据持久化
- JSON export/import：数据交换格式

---

## 验收标准
1. 可创建 3×3、4×4、5×5、6×6 不同尺寸棋盘
2. 每格可设置类型、颜色、名称、地产价格/过路费
3. 3D 实时预览与游戏内渲染一致
4. 保存/导出/导入 JSON 功能正常
5. 预设模板可一键加载
6. 设为默认地图后新游戏使用该地图
